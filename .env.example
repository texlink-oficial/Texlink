# ===========================================
# TEXLINK - Root Environment Variables
# ===========================================
# Copy this file to .env and fill in the values
# NEVER commit .env to version control
#
# These variables are used by docker-compose.yml and docker-compose.staging.yml

# ===========================================
# PostgreSQL
# ===========================================
# Password for the postgres container.
# Generate a strong random password:
#   openssl rand -base64 32
POSTGRES_PASSWORD=change_me_in_production

# ===========================================
# Redis (Staging only — docker-compose.staging.yml)
# ===========================================
# Password for the Redis container.
# Generate a strong random password:
#   openssl rand -base64 32
REDIS_PASSWORD=change_me_in_production

# ===========================================
# Backend service
# ===========================================
# When running the backend locally against Docker containers, set:
#   DATABASE_URL=postgresql://texlink:${POSTGRES_PASSWORD}@localhost:5432/texlink
#   REDIS_URL=redis://:${REDIS_PASSWORD}@localhost:6380
#
# See backend/.env.example for the full list of backend environment variables
